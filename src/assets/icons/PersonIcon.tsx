import React from 'react';
import userImg from '../eka.png'

const PersonIcon = () => {
  return (
   	<img className='w-full h-full object-cover' src={userImg} /> 
  );
};

export default PersonIcon;
